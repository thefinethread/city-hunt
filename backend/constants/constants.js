const SERVER_ERROR = 'Internal Server Error';

const FAILURE_MSG =
  'Unfortunately, none of the cops were able to capture the fugitive. Better luck next time!';

module.exports = { SERVER_ERROR, FAILURE_MSG };
